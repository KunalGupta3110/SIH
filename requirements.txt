# IBVAP — Intelligent Border Video Analytics Platform (SIH 2026 PS 26187)
# Complete Dependencies List (CPU/GPU Compatible)

# Core Computer Vision & Deep Learning
ultralytics>=8.1.0
opencv-python>=4.8.0
numpy>=1.24.0
torch>=2.0.0
torchvision>=0.15.0
lapx>=0.5.5
scipy>=1.10.0
pillow>=10.0.0
imageio-ffmpeg>=0.4.9

# Backend API (backend/ — IBVAP Sentinel core: database, threat engine,
# correlation, evidence ledger, hardware bridge, notifications)
fastapi>=0.110.0
uvicorn>=0.28.0
pydantic>=2.6.0
pyserial>=3.5      # boom-barrier / siren controller; backend runs in SIMULATION MODE without it
pytest>=8.0.0       # tests/test_sentinel.py
httpx>=0.27.0       # required by FastAPI's TestClient

# Command Dashboard
streamlit>=1.32.0
pandas>=2.1.0

# Utilities
tqdm>=4.65.0
requests>=2.31.0
