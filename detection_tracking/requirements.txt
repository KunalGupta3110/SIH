# IBVAP Detection & Tracking Dependencies
# CPU and GPU compatible (will use CUDA if available, falls back to CPU)

ultralytics>=8.1.0
opencv-python>=4.8.0
numpy>=1.24.0
torch>=2.0.0
torchvision>=0.15.0
lapx>=0.5.5
scipy>=1.10.0
tqdm>=4.65.0
